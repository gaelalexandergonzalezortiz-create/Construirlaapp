
  # Construir la app

  This is a code bundle for Construir la app. The original project is available at https://www.figma.com/design/VhmFqTqlPg4fiPrYiTybfR/Construir-la-app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  